#start ubuntu contaier for docker operations.
docker run -it -v /var/run/docker.sock:/var/run/docker.sock ubuntu:latest

#very important
apt update 

#install docker inside ubuntu/ you can download from official website latest one
apt install docker.io -y

